//! Value-semantic tests for the runtime dimension tag.
//!
//! The leaves divide by the contract under test: derivation from the law
//! crate's type parameters, the semantic marker that keeps dimensionally
//! identical quantities apart, the exponent algebra, and the wire form.

mod algebra;
mod derivation;
mod rendering;
mod semantics;
